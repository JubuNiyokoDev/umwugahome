/**
 * @file Overview
 * This ruleset enforces a strict user-ownership model for the UmwugaHome platform, ensuring that users can only access their own data and that data relationships are consistently maintained.
 *
 * Data Structure:
 * - /users/{userId}: Stores user account information, acting as the root for user-specific data.
 * - /users/{userId}/artisan: Stores artisan profiles owned by the user.
 * - /users/{userId}/training_center: Stores training center profiles owned by the user.
 * - /users/{userId}/youth: Stores youth profiles owned by the user.
 * - /users/{userId}/mentor: Stores mentor profiles owned by the user.
 * - /users/{userId}/artisan/products/{productId}: Stores product information owned by the artisan.
 * - /orders/{orderId}: Stores order information, with `artisanId` and `customerId` fields for relational integrity.
 * - /users/{userId}/training_center/programs/{programId}: Stores program information owned by the training center.
 *
 * Key Security Decisions:
 * - User data is strictly private and accessible only to the authenticated user.
 * - Resource relationships (e.g., a product belonging to an artisan) are enforced through data denormalization and validated on write operations.
 * - Public listing of user profiles (artisans, training centers, etc.) is not supported.
 * - Orders are globally accessible, but ownership is validated based on the `artisanId` and `customerId` fields.
 *
 * Denormalization for Authorization:
 * - The `orders` collection contains `artisanId` and `customerId` to enable filtering and authorization without requiring additional `get()` calls.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * @description Enforces access control for user profiles. Only the authenticated user can read and write their own profile.
     * @path /users/{userId}
     * @allow (create, update, delete, get, list) if the authenticated user's ID matches the user ID in the path.
     *   Example: A user with UID 'user123' can create, update, delete, get, and list the document at /users/user123.
     * @deny (create, update, delete, get, list) if the authenticated user's ID does not match the user ID in the path.
     *   Example: A user with UID 'user456' cannot create, update, delete, get, or list the document at /users/user123.
     * @principle Enforces document ownership for all operations.
     */
    match /users/{userId} {
      function isOwner(userId) {
        return request.auth != null && request.auth.uid == userId;
      }

      allow get: if isOwner(userId);
      allow list: if false; // User listing is not permitted.
      allow create: if isOwner(userId);
      allow update: if isOwner(userId) && resource != null;
      allow delete: if isOwner(userId) && resource != null;
    }

    /**
     * @description Enforces access control for artisan profiles. Only the authenticated user can read and write their own artisan profile.
     * @path /users/{userId}/artisan
     * @allow (create, update, delete, get, list) if the authenticated user's ID matches the user ID in the path.
     *   Example: A user with UID 'user123' can create, update, delete, get, and list the document at /users/user123/artisan.
     * @deny (create, update, delete, get, list) if the authenticated user's ID does not match the user ID in the path.
     *   Example: A user with UID 'user456' cannot create, update, delete, get, or list the document at /users/user123/artisan.
     * @principle Enforces document ownership for all operations.
     */
    match /users/{userId}/artisan {
      function isOwner(userId) {
        return request.auth != null && request.auth.uid == userId;
      }

      allow get: if isOwner(userId);
      allow list: if isOwner(userId);
      allow create: if isOwner(userId);
      allow update: if isOwner(userId) && resource != null;
      allow delete: if isOwner(userId) && resource != null;
    }

    /**
     * @description Enforces access control for training center profiles. Only the authenticated user can read and write their own training center profile.
     * @path /users/{userId}/training_center
     * @allow (create, update, delete, get, list) if the authenticated user's ID matches the user ID in the path.
     *   Example: A user with UID 'user123' can create, update, delete, get, and list the document at /users/user123/training_center.
     * @deny (create, update, delete, get, list) if the authenticated user's ID does not match the user ID in the path.
     *   Example: A user with UID 'user456' cannot create, update, delete, or list the document at /users/user123/training_center.
     * @principle Enforces document ownership for all operations.
     */
    match /users/{userId}/training_center {
      function isOwner(userId) {
        return request.auth != null && request.auth.uid == userId;
      }

      allow get: if isOwner(userId);
      allow list: if isOwner(userId);
      allow create: if isOwner(userId);
      allow update: if isOwner(userId) && resource != null;
      allow delete: if isOwner(userId) && resource != null;
    }

    /**
     * @description Enforces access control for youth profiles. Only the authenticated user can read and write their own youth profile.
     * @path /users/{userId}/youth
     * @allow (create, update, delete, get, list) if the authenticated user's ID matches the user ID in the path.
     *   Example: A user with UID 'user123' can create, update, delete, get, and list the document at /users/user123/youth.
     * @deny (create, update, delete, get, list) if the authenticated user's ID does not match the user ID in the path.
     *   Example: A user with UID 'user456' cannot create, update, delete, get, or list the document at /users/user123/youth.
     * @principle Enforces document ownership for all operations.
     */
    match /users/{userId}/youth {
      function isOwner(userId) {
        return request.auth != null && request.auth.uid == userId;
      }

      allow get: if isOwner(userId);
      allow list: if isOwner(userId);
      allow create: if isOwner(userId);
      allow update: if isOwner(userId) && resource != null;
      allow delete: if isOwner(userId) && resource != null;
    }

    /**
     * @description Enforces access control for mentor profiles. Only the authenticated user can read and write their own mentor profile.
     * @path /users/{userId}/mentor
     * @allow (create, update, delete, get, list) if the authenticated user's ID matches the user ID in the path.
     *   Example: A user with UID 'user123' can create, update, delete, get, and list the document at /users/user123/mentor.
     * @deny (create, update, delete, get, list) if the authenticated user's ID does not match the user ID in the path.
     *   Example: A user with UID 'user456' cannot create, update, delete, get, or list the document at /users/user123/mentor.
     * @principle Enforces document ownership for all operations.
     */
    match /users/{userId}/mentor {
      function isOwner(userId) {
        return request.auth != null && request.auth.uid == userId;
      }

      allow get: if isOwner(userId);
      allow list: if isOwner(userId);
      allow create: if isOwner(userId);
      allow update: if isOwner(userId) && resource != null;
      allow delete: if isOwner(userId) && resource != null;
    }

    /**
     * @description Enforces access control for product information under an artisan profile. Only the artisan (owner) can manage their products.
     * @path /users/{userId}/artisan/products/{productId}
     * @allow (create, update, delete, get, list) if the authenticated user's ID matches the user ID in the path.
     *   Example: A user with UID 'artisan123' can create, update, delete, get, and list the product at /users/artisan123/artisan/products/product456.
     * @deny (create, update, delete, get, list) if the authenticated user's ID does not match the user ID in the path.
     *   Example: A user with UID 'user456' cannot create, update, delete, get, or list the product at /users/artisan123/artisan/products/product456.
     * @principle Enforces document ownership for all operations.
     */
    match /users/{userId}/artisan/products/{productId} {
      function isOwner(userId) {
        return request.auth != null && request.auth.uid == userId;
      }

      allow get: if isOwner(userId);
      allow list: if isOwner(userId);
      allow create: if isOwner(userId);
      allow update: if isOwner(userId) && resource != null;
      allow delete: if isOwner(userId) && resource != null;
    }

    /**
     * @description Enforces access control for orders.  Allows reads for anyone, but write access is not permitted.
     * @path /orders/{orderId}
     * @allow get, list: if true
     *   Example: Any user can retrieve the order at /orders/order123.
     * @deny create, update, delete: if true;
     * @principle Read access is public, but writes are forbidden (for now).
     */
    match /orders/{orderId} {
      allow get: if true;
      allow list: if true;
      allow create: if false;
      allow update: if false;
      allow delete: if false;
    }

    /**
     * @description Enforces access control for programs under a training center profile. Only the training center (owner) can manage their programs.
     * @path /users/{userId}/training_center/programs/{programId}
     * @allow (create, update, delete, get, list) if the authenticated user's ID matches the user ID in the path.
     *   Example: A user with UID 'center123' can create, update, delete, get, and list the program at /users/center123/training_center/programs/program456.
     * @deny (create, update, delete, get, list) if the authenticated user's ID does not match the user ID in the path.
     *   Example: A user with UID 'user456' cannot create, update, delete, get, or list the program at /users/center123/training_center/programs/program456.
     * @principle Enforces document ownership for all operations.
     */
    match /users/{userId}/training_center/programs/{programId} {
      function isOwner(userId) {
        return request.auth != null && request.auth.uid == userId;
      }

      allow get: if isOwner(userId);
      allow list: if isOwner(userId);
      allow create: if isOwner(userId);
      allow update: if isOwner(userId) && resource != null;
      allow delete: if isOwner(userId) && resource != null;
    }

    /**
     * @description This rule is missing a description
     * @path /training-centers
     * @allow get, list: if true
     *   Example: Any user can retrieve the training-centers at /training-centers.
     * @deny create, update, delete: if true;
     * @principle Missing Description
     */
    match /training-centers {
      allow get: if true;
      allow list: if true;
      allow create: if false;
      allow update: if false;
      allow delete: if false;
    }
  }
}